<?xml version="1.0" encoding="UTF-8"?>
<!-- Sitemaps require absolute URLs, so this cannot be domain-agnostic.
     Swap the host when a custom domain lands.
     One page, so one entry — the in-page anchors are not separate URLs. -->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://coach-swapnil.netlify.app/</loc>
    <changefreq>monthly</changefreq>
    <priority>1.0</priority>
  </url>
</urlset>
